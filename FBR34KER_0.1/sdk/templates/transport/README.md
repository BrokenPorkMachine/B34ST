# {{NAME}} transport adapter

This adapter must expose only an authorized FBDP endpoint. It must not add exploit entry, arbitrary physical-memory access, unbounded retries, or bypass target-region policy.
